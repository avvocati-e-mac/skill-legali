# N04 — SearXNG-skill — async/await Python 3.12 vs 3.10

**Header skill**: `[informatica · deep · 5 fonti · en · nessun filtro]`

**Classificazione**: informatica / deep → EN, readHeadings→section su docs ufficiali.

**Fonti**: docs.python.org/3/whatsnew/3.12.html (ufficiale, scelta), medium pythonworld, andy-pearce, dev.to.

**Lettura URL**:
- `section:"asyncio"` su docs ufficiali → **estratto**: scrittura su socket asyncio significativamente più veloce
  (sendmsg, no copie inutili); nuove `asyncio.eager_task_factory()` / `create_eager_task_factory()` (eager task
  execution, 2x-5x più veloce in alcuni casi).

**Risposta**: in Python 3.12 async/await resta sintatticamente invariato vs 3.10, ma asyncio migliora: I/O su socket
più veloce (sendmsg, zero-copy), eager task factory (2-5x su alcuni use-case), + miglioramenti generali performance
3.12. La sintassi `async def`/`await`/`asyncio.gather` è la stessa; cambiano internals e nuovi opt-in. Fonte: docs ufficiali.

**Tool call**: 1 search + 1 `web_url_read` (section "asyncio"). **Chars**: ~3.000 + ~900 ≈ 3.900.
**Lingua**: EN, fonte docs.python.org ufficiale. ✅
**Verifiability**: claim da fonte primaria (docs ufficiali Python). ✅ massima autorevolezza.
